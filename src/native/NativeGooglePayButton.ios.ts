// iOS stub — BoltGooglePayButton is not registered on iOS (Android only).
// Metro resolves this file on iOS instead of NativeGooglePayButton.ts,
// preventing codegenNativeComponent('BoltGooglePayButton') from being called.
export default null as any;
