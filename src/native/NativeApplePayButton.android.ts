// Android stub — BoltApplePayButton is not registered on Android (iOS only).
// Metro resolves this file on Android instead of NativeApplePayButton.ts,
// preventing codegenNativeComponent('BoltApplePayButton') from being called.
export default null as any;
